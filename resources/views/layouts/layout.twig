<!DOCTYPE html>
<html lang="{{ config('app.locale') }}">
    <head>
        <meta charset="utf-8">
        <meta name="robots" content="noindex,nofollow">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, shrink-to-fit=no, initial-scale=1">
        <meta name="description" content="">
        <meta name="author" content="{{ "asd" }}">
        <meta name="csrf-token" content="{{ csrf_token() }}">

        <title>{{ config('app.name') }}</title>

        <!-- Bootstrap Core CSS -->
        <link href="{{ asset('css/app.css') }}" rel="stylesheet">
        <link href="{{ asset('css/main.css') }}" rel="stylesheet">
        <link href="{{ asset('css/datatables-bootstrap3.css') }}" rel="stylesheet">
        {#<link href="{{ asset('css/mat.min.css') }}" rel="stylesheet">#}
        <link rel="stylesheet" href="{{ asset('css/_all.css') }}">

        {% block styleSheets %}
        {% endblock %}

        <script type="application/javascript">
            window.Laravel = {csrfToken: '{{ csrf_token() }}'};
        </script>
    </head>

    <body>
        {% if hasSideBar %}
        <div id="wrapper">
        {% endif %}

            {% block sidebar %}
            {% endblock %}

            {% block content %}
            <!-- Page Content -->
            <div id="page-content-wrapper">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-lg-12">
                            <a href="#menu-toggle" class="btn btn-default" id="menu-toggle">Toggle Menu</a>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /#page-content-wrapper -->
            {% endblock %}

        {% if hasSideBar %}
        </div>
        <!-- /#wrapper -->
        {% endif %}

        {#<script src="{{ asset('js/tether.js') }}" type="application/javascript"></script>#}
        <script src="{{ asset('js/app.js') }}" type="application/javascript"></script>

        {% block pageJs %}{% endblock %}
        {% block inlineJs %}{% endblock %}

        {% block debug %}
            <script src="{{ asset('js/icheck.js')}}"></script>
            {#<script type="application/javascript" src="{{ asset('js/mat.min.js') }}"></script>#}
        {#{{ dump() }}#}
        {% endblock %}

        <script type="application/javascript">
            $(function () {
               /* $('input').iCheck({
                    checkboxClass: 'icheckbox_minimal',
                    radioClass: 'iradio_minimal',
                    increaseArea: '-10%' // optional
                });*/
            });

            $(window).on('load', function() {
                //$.material.init();
            });
        </script>
    </body>
</html>